<div class="row">
  <div class="col-6">
    <h1><%= @pet.name %></h1>
    <p><%= @pet.species %></p>
    <p><%= @pet.last_seen_address %></p>
    <p><%= @pet.last_seen_at %></p>
    <p><%= @pet.found_address %></p>
    <p><%= @pet.found_at %></p>
    <p><%= @pet.found %></p>

    <%= link_to "Back to index", root_path, class: "btn btn-warning" %>
  </div>
</div>